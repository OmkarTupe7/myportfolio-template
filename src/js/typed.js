import Typed from 'typed.js';

var options = {
  strings: ["I'm a Front-end Developer", 'I Build  Websites'],
  typeSpeed: 150,
  backSpeed: 50,
  backDelay: 2000
};

var typed = new Typed('#headline', options);
